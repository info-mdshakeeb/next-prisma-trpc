import { ContentSection } from "@/components/layout";

export default function page() {
  return (
    <ContentSection
      title="Profile"
      desc="This is how others will see you on the site."
    >
      <div className="h-screen ">Account Settings Page</div>
    </ContentSection>
  );
}
